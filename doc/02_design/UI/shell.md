# AppShell（全体骨格）

| 項目 | 内容 |
|------|------|
| 情報設計 | [`../UI_design.md`](../UI_design.md)（画面遷移・ナビ優先） |
| ビジュアル横断 | [`visual.md`](./visual.md) |
| 更新日 | 2026-08-09 |

---

## レイアウト

| 項目 | 決定 |
|------|------|
| デバイス | **デスクトップ優先**（主に Windows、macOS 対応） |
| 全体 | 左ナビ ＋ ヘッダ（タイトル / 更新 / 最終更新）＋ MainContent |
| 読取方向 | 上→下。分析本文は1カラム縦積み（MVP 固定） |
| 起動 | MainContent に **S00** をマウント |

```text
┌──────────────────────────────────────────────────────────┐
│ EquiScout          [更新]  最終更新: YYYY-MM-DD HH:mm     │  ← ヘッダ
├──────────┬───────────────────────────────────────────────┤
│ ホーム   │                                               │
│ 募集馬   │              MainContent                      │  ← 各 Screen
│ 調教師   │                                               │
│ 生産牧場 │                                               │
│ 血統     │                                               │
│ ──────── │                                               │
│ 設定     │                                               │
└──────────┴───────────────────────────────────────────────┘
     ↑ グローバルナビ（先頭＝ホーム）
```

```text
AppShell
├─ HeaderBar（タイトル / UpdateShortcut / UpdateStatusChip）
├─ GlobalNav（NavItem × 画面。先頭は S00）
└─ MainContent（Screen を1つマウント。起動時は S00）
```

### ヘッダの更新

- [更新] は S00 / S06 の更新と同じ UC（C08）
- 更新中はヘッダに進行表示し、MainContent はブロックしない

### ナビ項目（MVP）

ホーム / 募集馬 / 調教師 / 生産牧場 / 血統 / 設定  
（複数頭比較は出さない）

---

## ビジュアル

| 項目 | 方針 |
|------|------|
| ブランド | ヘッダに「EquiScout」を常時表示 |
| ヘッダ進捗 | 控えめだが無視できない |
| ナビ | 現在画面が分かるコントラスト。装飾過多にしない |
| 狭幅 | 1カラム折りたたみを許容（詳細トークンは `base_desing.md`） |
